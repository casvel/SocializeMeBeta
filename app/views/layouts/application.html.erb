<!DOCTYPE html>
<html>
<head>
  <title>SocializeMeBeta</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if signed_in?(:user) %>
    Signed in as <strong><%= current_user.email %></strong>. Not you?
    <a data-method='delete' href = "/users/sign_out#delete">
      <button type="submit" class="btn btn-default">Sign out</button>
    </a>
<% else %>
    <strong> Sign in with: </strong>
    <a href = "auth/twitter" class = "auth_provider">
      <%= image_tag "twitter_32.png", :size => "32x32", :alt => "Twitter" %>
    </a>
<% end %>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
